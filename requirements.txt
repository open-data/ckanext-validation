ckantoolkit>=0.0.3
# we use a custom goodtables fork (canada fork only)
#goodtables==1.5.1
# servers cannot call to github (canada fork only)
#-e git+https://github.com/ckan/ckanext-scheming.git#egg=ckanext-scheming
ckanapi
